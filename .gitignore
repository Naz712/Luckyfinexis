# Secrets — the service_role key must never enter the repo
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Server keys (tracker/server/.env is also ignored by its own .gitignore)
tracker/server/.env
